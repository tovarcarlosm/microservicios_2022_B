server:
  port: 7979

eureka:
  instance:
    hostname: localhost
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url: https://${eureka.instance.hostname}:${server.port}/eureka/
